<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ==================== Permissions ==================== -->

    <!-- Network. Required by every plugin that talks to a server. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- For Razorpay's offline detection + general "no network" toasts. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Foreground location. FINE for GPS-accurate coordinates while
         dispatched, COARSE as the Android 12+ baseline. We do NOT request
         BACKGROUND_LOCATION — the driver app only pushes location while
         in the foreground and on an active trip. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Android 13+ requires this for any notification. Not used yet
         (foreground polling does our job today), but cheap to declare
         so the future FCM upgrade doesn't need a manifest change. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- ==================== Feature declarations ====================
         Marking location features as NOT required keeps the app installable
         on tablets / devices without a GPS chip — important for trainees
         signing up on shared family devices.
    -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false"/>

    <application
        android:label="ShareCab Driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Google Maps Platform API key — required by google_maps_flutter
             on Android. Replace with a Cloud-Console-restricted Android
             key (package id + SHA-1 fingerprint) before submission. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="***REMOVED_GOOGLE_MAPS_KEY***"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- ==================== Package visibility (Android 11+) ==================== -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
