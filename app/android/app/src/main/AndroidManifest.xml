<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by Google Maps SDK + http client. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Required by `geolocator`. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="sharecab"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Google Maps Platform API key — required by google_maps_flutter on Android.
             This is the same demo key used in iOS Info.plist; restrict it in the
             Cloud Console (Android: package + SHA-1) before going public, and rotate
             it after the demo period. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBWOhx-MTodQ3_mgUgi9ZTPK0ths1XaSNk"/>

        <!-- AdMob — required by google_mobile_ads to initialise. The
             value below is AdMob's OFFICIAL test app id (documented at
             developers.google.com/admob/android/test-ads). Replace
             with your real `ca-app-pub-*` id before going to prod.
             Test app id is harmless in production but always serves
             test ads — never monetises until swapped. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
